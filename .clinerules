# Cline Rules for baserCMS

## 注意事項

このファイルは参照用として保持されていますが、最新かつ詳細な開発ガイドラインは `.github/
instructions/` 配下に移動しました。
開発作業を行う際は、`.github/instructions/` 配下のファイルを参照してください。

## 開発ガイドライン
GitHub Copilot をはじめとする開発ツールは、`.github/instructions/` 配下のファイルを参照してコード提案やガイドラインの適用を行います。